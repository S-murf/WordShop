class Mapper:
    """This module takes a user-input string and chunks it into formatted tuples which
contains relevant data for all essential pieces of string."""

    def maptolist(arg, langbound):
        # Applies basic params to internalmap. Different maptolist() will have
        # different overloads.
        pass

    def _internalmap():
        # Takes k = langbound.split(arg)
        # Takes langbound.getlangcontext(k)
        # Combines this and additional data into tuple chunks.
        pass